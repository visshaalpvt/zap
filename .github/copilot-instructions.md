# Project instructions

- Keep the scanner limited to explicitly authorized targets.
- Preserve localhost-only ZAP exposure unless the user explicitly requests a secured deployment change.
- Validate CLI/config behavior without starting active scans in automated checks.
